#include <common.h>
#include <fs.h>

file_t* fopen(char* name, ...)
{
    return null;
}

uint32_t fread(file_t* afile, uint8_t* buffer, uint32_t count)
{
    return 0;
}

void fwrite(file_t* afile, uint8_t* buffer, uint32_t count)
{

}

void fclose(file_t* afile)
{

}